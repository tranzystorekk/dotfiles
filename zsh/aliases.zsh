# common aliases

# directory commands
alias md="mkdir -p"
alias rd=rmdir

# better ls
alias ls="ls --color=auto"
alias ll="ls -l"
alias la="ls -la"

# cd on steroids
alias ..="cd .."
alias ...="cd ../.."

# nvim readonly
alias nview="nvim -R"
